export enum ActionGetUsers {
    all = 'all',
    single = 'single',
    by_bumber = 'by_bumber',
    by_manage = 'by_manage',
}

export enum Sexe {
    M = "MASCULIN",
    F = "FEMININ"
}
export enum StatutClient {
    actif = "ACTIF",
    inactif = "INACTIF"
}